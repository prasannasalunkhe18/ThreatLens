# GitHub personal access token (read-only: public_repo or repo scope)
GITHUB_TOKEN=

# Groq API key (preferred free model, tried first) https://console.groq.com/keys
GROQ_API_KEY=

# OpenRouter API key (free model fallbacks) https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Optional: seconds to wait between finding investigations (default 2)
# THREATLENS_LLM_DELAY=2
